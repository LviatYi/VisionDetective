[package]
name = "VisionDetective"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.18.1", default-features = false, features = ["2d", "default_font", "debug"] }
serde = { version = "1.0", features = ["derive"] }
toml = "0.9"
